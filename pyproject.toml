[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "quickdev"
version = "0.1.0"
description = "Analisis de feedback de playtesters: el modelo interpreta lenguaje, el codigo verifica hechos."
requires-python = ">=3.12"
# `readme = "README.md"` va aqui cuando core/jose cree el archivo. Declararlo
# antes rompe el build: hatchling verifica que el fichero exista.
authors = [{ name = "Equipo Solid" }]

# Versiones fijadas con ==, no con >=. Un baseline de evals no es comparable si
# el entorno flota: si manana el SDK cambia de comportamiento, la medicion
# guardada deja de significar lo mismo y no hay nada en el repo que lo explique.
dependencies = [
  "google-genai==2.18.1",
  "pydantic==2.13.4",
  "pydantic-settings==2.15.0",
  "pandas==3.0.5",
  "python-dotenv==1.2.2",
]

[project.optional-dependencies]
dev = [
  "pytest==9.1.1",
  "ruff==0.16.7",
  "nbstripout==0.9.1",
]

[tool.hatch.build.targets.wheel]
packages = ["quickdev"]

# ---------------------------------------------------------------------------
# pytest
# ---------------------------------------------------------------------------
[tool.pytest.ini_options]
testpaths = ["tests"]
# Los tests marcados `llm` gastan cuota de API y dependen de la red. Quedan
# deseleccionados por defecto: una suite que cuesta dinero no se corre, y una
# suite que no se corre no protege nada. Para correrlos: pytest -m llm
addopts = "-m 'not llm' --strict-markers -q"
markers = [
  "llm: llama a la API de Gemini (deseleccionado por defecto; usa -m llm)",
]

# ---------------------------------------------------------------------------
# ruff
# ---------------------------------------------------------------------------
[tool.ruff]
line-length = 100
target-version = "py312"

# Quedan dos de las tres exenciones. `evals/motor.py` y `evals/casos.py` ya no
# existen: los reemplazo core/miguel por cases/runner/diagnosis/reporting, que
# pasan ruff limpios, asi que sus lineas se borraron aqui.
#
# La del notebook NO se puede borrar todavia, y no es un olvido. ruff lintea
# .ipynb por defecto, el notebook conserva sus 28 violaciones preexistentes y lo
# vacia core/jose (decision D1), que mergea DESPUES que core/miguel. Borrarla
# ahora dejaria `ruff check .` en rojo por un archivo de otra rama. La borra
# core/jose en el mismo commit en que vacie el notebook.
exclude = [
  "QuickDev_01.ipynb",  # dueno: core/jose
]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "RUF"]

[tool.ruff.lint.per-file-ignores]
# Permiso anticipado: cuando las ramas anaden re-exports de conveniencia a los
# __init__ (`from .models import FeedbackReport`), F401 los marcaria como
# import sin usar. Hoy ningun __init__ importa nada, asi que esta linea no
# silencia nada todavia.
"quickdev/**/__init__.py" = ["F401"]
