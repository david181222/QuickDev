"""Las reglas de validacion deterministas.

Una regla = una clase = una razon para cambiar. `base.py` define el contrato;
`registry.py` compone las versiones. Las reglas concretas van en los modulos
tematicos (counts, citations, versioning, enums, review), que implementa
`core/edwin`.
"""
